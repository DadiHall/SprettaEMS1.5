<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :content %>
	<%= f.text_area :content %>

	<%= f.label :tag_list, "Tags (seperated by commas)" %>
	<%= f.text_area :tag_list %>


	<%= f.submit %>

<% end %>